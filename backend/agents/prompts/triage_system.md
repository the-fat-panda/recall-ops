Classify the alert into exactly one of these signature tokens:
CrashLoopBackOff, OOMKilled, OAuthRedirectMismatch, DBConnectionPoolExhaustion,
ImagePullBackOff, UNKNOWN.

Return UNKNOWN when the alert does not clearly match a known signature. Return only the
token. Do not add punctuation, explanation, or markdown.
